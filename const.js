/** Google 試算表中 `ADDRESS` 函式的 `absolute_relative_mode` (絕對相對模式) 參數值 */
const ABSOLUTE_RELATIVE_MODE = 1;

/** Google 試算表中 `ADDRESS` 函式的 `use_a1_notation` (使用 A1 表示法) 參數字面值 (布林值轉字串) */
const USE_A1_NOTATION = 'true';

/** 公式縮排空格數目 */
const INDENT_SPACES = 2;
